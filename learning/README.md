# 動態デザイン研究室 学習資料

研究に必要な知識を学ぶためのポータルと、Reactでつくったインタラクティブ教材です。

公開先：<https://design-for-changes.github.io/lab-learning/learning/>

## 教材

**統計解析入門** の構成は以下のとおりです。

1. 統計解析とデータの基礎：記述と推測、母集団・標本、平均とばらつき、標準誤差、外れ値、ヒストグラムから確率密度、代表的な確率分布。
2. 推定と検定：一人ずつの差とばらつきから、カテゴリー別の観測値と母集団モデルの図、帰無仮説へ。等分散の比較図とF検定を先に学び、t検定、12人の検定結果、t分布の面積からp値を読む。その後に信頼区間、一元配置・二元配置分散分析の結果表と読み方。ヒストグラム・Q–Qプロット・残差の例と、計算から考察までの例題。
3. データと変数：目的変数・説明変数、目的変数を置かない解析、尺度、対応と独立、直線・頭打ち・U字・波の図から線形と非線形を考える。
4. 実験計画法：目的から確認実験までの6手順。完全要因計画の負担から、直交表で実験数を減らす考え方へ。L4の行列の読み方、2列の釣り合い、行平均の計算、見分けられなくなる効果を、同じ画面の実験で学ぶ。代表的な直交表の一覧と、L4・L8の線点図で列への割り付けを説明する。24人の架空データから主効果・交互作用・三元配置分散分析を計算し、CSVとPython実行例も用意する。
5. 解析を選ぶ・調べる：多変量解析と条件比較の選択樹形図だけを、タブを使わず続けて表示。一元配置・二元配置・多元配置を具体例で区分し、数量化理論I〜IV類を含む27手法の詳しい解説へ進む。
6. 解析の注意点：「やってはいけないこと」「注意するべきこと」。各項目の理由・具体例・確認方法と、多重共線性への対処。
7. 実施する方法：統計ソフト・表計算・Python・Rの無料／有料／条件付き無料の区分、Pythonのライブラリ一覧と実行環境。

各章は入門の説明と一覧を基本とし、図やAIへの質問例を補助に置いています。Pythonの実習は「実施する方法」から開く練習例として残しています。

平均と中央値、対応する測定値、PCAの軸、ヒストグラムの密度への換算と推定曲線、正規分布の面積、一元配置分散分析、交互作用、直交表、説明変数の相関とVIFを操作できる図を含みます。教材の図とCSVは架空の例で、個人データを含みません。解析選択は候補を検討するための入口であり、すべての研究設計を網羅するものではありません。

学習資料には統計解析入門、人工知能学習入門、マネジメント入門、進化生物学入門、脳科学入門、心理学入門、デザイン学入門、科学入門、経済学入門、AI入門を用意しています。AI入門は一覧の11番目です。ウェブインタラクション入門は準備中として配置しています。

## ページのURLと共有時の表示

各章は `/lab-learning/learning/ai-intro/partnership/` のようなページ別URLを持ちます。`scripts/build-pages.mjs` が全ルートの本文と共有用メタ情報をHTMLに出力するため、JavaScriptを実行しない共有サービスにも章名と科目名が伝わります。旧 `#/...` URLは、開くと対応するページ別URLへ移動します。共有には移動後のURLを使います。すでに投稿された旧URLのプレビューは、投稿先のキャッシュや再取得の仕様に依存します。

本文のリンクは `src/PageLink.jsx` を使います。既存の `href="#/…"` 指定をページ別URLへ変換し、外部リンク・資料のダウンロード・ページ内アンカーは維持します。ルート一覧は `App.jsx` の `learningPaths`、共有タイトルは本文の見出しと科目名から生成します。新しい章もこの一覧に含めてください。`npm run build` では、生成したHTMLのタイトル、共有用情報、内部リンクとファイルを確認します。

## 作業

Node.js 24以降を推奨します。

```sh
npm ci
npm run dev
```

ローカルプレビュー：<http://127.0.0.1:4178/lab-learning/learning/>

```sh
npm run check
npm run build
```

`check` は図の計算、架空データと表示の一致、選択フローの全分岐、各ページのレンダリング、内部リンクと教材ダウンロードを確認します。ブラウザーの見た目やクリック操作の検査とは別です。

## 公開

`main` へのpushでGitHub Actionsが検証・ビルドし、`dist/`のみをGitHub Pagesへ公開します。GitHub PagesはGitHub Actionsによる公開に設定します。

Viteのbaseは `/lab-learning/learning/`。ページの切り替えにはハッシュを使うので、各教材のURLを直接開いたり更新したりできます。サーバーやAPIキーは不要です。`/statistics` は中間ポータルを挟まず01を開きます。旧 `/statistics/distributions` は、分布の説明を統合した `/statistics/basics` と同じ教材を表示します。旧 `/statistics/methods` は統合した `/statistics/choose` と同じ内容を表示します。左メニューは章の移動に絞り、手法は05のチャートから開きます。

## 編集する場所

- `src/App.jsx`：ポータル、基礎、変数、手法ページ、ナビゲーション
- `src/content.js`：手法の説明と出典
- `src/chooser.js`：旧フォームの分岐ロジック（現在の画面では未使用）
- `src/Chooser.jsx`、`src/AnalysisMap.jsx`：タブを使わない選択チャート
- `src/RelationshipExplorer.jsx`：直線・頭打ち・U字・波の関係の図
- `src/DiagnosticFigures.jsx`、`src/diagnosticExamples.js`：同じ残差データを使う3つの診断図
- `src/WorkedAnovaExample.jsx`：一元配置分散分析の例題・計算・考察
- `src/Explorers.jsx`、`src/math.js`：動く図と計算
- `src/DistributionLesson.jsx`：基礎ページ内の分布と確率
- `src/InferenceLesson.jsx`、`src/InferenceExplorers.jsx`：02の推定・検定・分散分析
- `src/TTestFigures.jsx`、`src/pairedTExample.js`：等分散の比較図、対応のあるt検定の計算結果とp値の面積図
- `src/PopulationComparison.jsx`、`src/populationComparison.js`：同じ観測値を共通・別々の母集団モデルで考える図
- `src/ExperimentalDesignLesson.jsx`、`src/DesignExplorers.jsx`：04の実験計画法と直交表
- `src/LinearGraphGuide.jsx`：L4・L8の線点図と列の対応
- `src/designExamples.js`：L4の設定名・架空の行平均と比較計算
- `src/experimentMath.js`、`src/histogram.js`：実験計画・分散分析・密度の図の計算
- `src/ChecksLesson.jsx`：解析の注意点
- `src/ToolsLesson.jsx`：実施する方法とライブラリ一覧
- `src/PythonLesson.jsx`：Pythonの練習例
- `src/style.css`：共通グリッドとレスポンシブ表示。文字サイズはrem、ルートは100%。
- `public/data/`：練習用CSV、Pythonコード、ローカルJupyter用ノート

Pythonの確認用コードを変えた場合は、ノート内の確認用コードも更新します。教材の数値はSciPyによる対応のあるt検定で確認しています。

## 補助機能の検証状況

05はチャート表示に絞ったため、旧選択フォームとWebMCPツールの登録は行いません。教材の実行手順は各自のPCのPythonを前提とし、計算コードもローカルのPython環境で確認しています。

デスクトップ幅・スマートフォン幅で、主要ページの表示と横にはみ出さないことを確認しています。正規分布の区間変更、多重共線性の相関スライダー、解析選択の操作もブラウザーで確認しています。

## 手法別の入力・出力・Python例

27手法の各ページは、必要な列と入力表、実際にPythonで得た出力、結果の読み方、AIへの依頼例、確認用の実行コードの順で構成しています。`src/methodGuides.js`が解説、`src/methodExampleResults.json`が実行結果、`public/data/method-examples/`がCSVとPythonコードです。例はすべて架空データであり、人数は必要標本数の目安ではありません。

主要な出力と判断の対応は `src/AnalysisOutputReading.jsx` にまとめています。PCAは固有値・寄与率・累積寄与率と軸数の選び方、分散分析は効果量とTukey HSD、回帰は係数の不確かさとモデル全体の当てはまりなどを、実際の計算結果で確認できます。

数量化理論III類・多重対応分析は、60人分の架空の回答を0/1に変換して固有値問題を解きます。カテゴリーと回答者の図を分け、回答パターンを選ぶと対応する点を強調します。`src/CategoryPatternPlots.jsx` が図、追加の `*-scores.csv` が全員のサンプルスコアです。

例の元コードを変更する場合は、必要なPython依存関係を入れた環境で `python scripts/build-method-examples.py` を学習アプリのディレクトリから実行し、出力を再生成してください。使用バージョンは生成JSONと各ページに記録します。`npm run check --workspace learning` はコード・CSV・生成結果の整合と全ページを確認します。

t-SNEはIII類と同じ60人の回答を使い、4設定の座標・図と元の回答を照合します。計算結果とダウンロード用Pythonは他の手法と同じ生成スクリプトで更新します。

## 人工知能学習入門

`#/ai`は01へ直接進みます。離散数学、グラフとマイニング、確率と学習の種類、ニューラルネットワーク、重みの学習と過学習、ローカルPythonの実行例、ニューラルネットワークの応用、転移学習・知識蒸留の8章です。08の末尾から、続きの教材「AI入門」へ進めます。`src/AiCourse.jsx`が章の一覧と基礎の本文、`src/AiExplorers.jsx`が操作する図、`src/aiMath.js`がグラフ・順伝播・誤差逆伝播・勾配降下法の計算です。

06の本文は `src/AiPracticeLesson.jsx`。05と同じXORを、目的→4通りのデータ→PCにさせる処理→ファイル保存と実行→答え合わせ→100回と5000回の比較→考察の順で追います。別の入力・最適化法の実験を並べず、4通りへの適合と未知データの評価を区別します。関係のCSVを集計するPython練習は02に配置しています。

`python scripts/build-ai-examples.py`で、関係・共起のCSVとPythonコード、XORの実行結果を再生成します。XORのコードの編集元は `public/data/ai/xor.py` です。XORは4行の真理値表を学ぶ例で、未知データの性能評価とは区別しています。チェックでは9個のパラメーターの勾配を有限差分と照合し、ブラウザー用計算と独立したNumPy実行結果も比較します。

過学習の例は `python scripts/build-overfitting-example.py` で再生成します（NumPy、pandas、scikit-learnが必要）。既存の160件を90件の学習用・30件の検証用・40件のテスト用に分け、学習用と検証用だけを比較します。テスト用の結果は停止時点や設定の選択に使いません。`src/OverfittingLesson.jsx`が表示、`src/overfittingExample.json`が実測した学習履歴です。

07は `src/AiArchitecturesLesson.jsx` と `src/ArchitectureExplorers.jsx`、08は `src/AiAdvancedCourse.jsx`。07「ニューラルネットワークの応用」は、06のスイッチ2個の予測から画像を読む課題へ進み、全結合で何に困るか→畳み込みとCNN→文章の順序とRNN・Transformer→正解ラベルのない画像とオートエンコーダ→新しい画像の生成とGANを扱います。各節は困る場面と工夫を説明し、細かな計算は閉じた補足に置きます。CNN・注意の重みは手設定であること、08の蒸留の確率表は説明用の仮の値であることを表示します。学習の手がかりと構造の名前を同列の択一にしません。

オートエンコーダの例は `python scripts/build-autoencoder-example.py` で再生成します（NumPy、scikit-learnが必要）。`public/data/ai/autoencoder.py` を実行し、手書き画像1437枚を64→16→64の全結合ネットワークで学習、別の360枚で復元を評価します。数字のラベルは使いません。`src/AutoencoderExample.jsx` は評価用の先頭3枚と実際の復元結果を表示し、計算値は `src/autoencoderExample.json` に記録しています。

07の `src/PoolingAndDepth.jsx` は最大プーリングの情報削減と層の図、`src/SequenceAndGanDiagrams.jsx` はRNNの隠れ状態、注意の重み付き和、ベクトル変換、TransformerのQ・K・V、GANの交互学習の図です。注意の図は既存の手設定ベクトルから計算します。RNN・GANの手順や特徴の形は模式図で、学習の実行結果とは区別します。

07の検算値は `python scripts/build-architecture-examples.py` で、配布用 `public/data/ai/architectures.py` をNumPyで実行して生成します。ブラウザーの計算と照合し、GANの勾配は有限差分でもチェックします。

## AI入門

学習資料の11番目に配置しています。`#/ai-intro`から01へ直接進みます。章の一覧と表示枠は `src/AiIntroCourse.jsx` です。

1. **現代的なAIの計算ロジック**（`#/ai-intro/systems`）：`src/AiSystemsLesson.jsx`。人工知能学習入門の続きとして、生成時の推論→ツールの要求と実行→ハーネス→コンテキストとRAG→検証を扱います。旧URL `#/ai/systems` も同じ章へつながります。
2. **AIの挙動特性**（`#/ai-intro/behavior`）：`src/AiBehaviorLesson.jsx`。ローカル／クラウドの作業領域、MCP、Computer useによる画面操作、ページ構造を使うブラウザ自動操作、記録による作業の継続を説明します。「作業する場所・道具との接続・操作の方法」を別の軸として整理します。News更新の図は構成例の説明で、実際のAI操作や公開は行いません。
3. **AIとの向き合い方**（`#/ai-intro/partnership`）：`src/AiPartnershipLesson.jsx`。目的・任せる範囲・資料と道具・結果の確認と調整という、AIとの仕事のマネジメントを扱います。「AIが仕事を進められる環境を用意する」「自分が使いやすい環境もAIとつくる」の両面を平易に説明します。開発例では、要件整理の文書を置く場所を用意し、最初に目的と範囲をある程度整理してから、小さく実装・確認し、文書とコード、確認用データ、記録を更新します。3段階で作業場の変化を見る図は架空の例で、実際のAI実行は行いません。プロンプト文例や操作練習は置かず、技術事例・WeightedMDS・研究の詳説は閉じた補足に置きます。

講演資料「AIのあれこれ」（蘆澤雄亮、2026年9月8日更新）は、本人の理解・問題意識・考え方と事例を知るための資料として扱います。用語の定義・一般的な動作仕様・効果の実証の根拠にはしません。02の技術説明はMCP、Anthropic、Playwrightの一次資料で確認し、個別実装と共通概念を区別します。03はアジャイル宣言の原則、Anthropicの開発環境の事例、指示ファイルの効果に関する比較研究（Gloaguenほか、2026年6月改訂版）も参照します。要件整理や環境の更新方法の効果を同研究が直接検証したとは扱わず、教材上の提案と確認された範囲を分けます。共通スタイルは `src/aiPartnership.css` です。

平均を求める6段階の操作図と `public/data/ai/harness_demo.py` は模擬モデルを使った教材で、外部のAIには接続しません。Pythonは標準ライブラリのみで実行できます。

## 進化生物学入門

`#/evolution` は01「前提（現代的な解釈）」を開きます。02「進化生物学の成立と発展」は研究者・理論・発見を年代順にたどる年表と、知見の関係を線で示す図、03「進化生物学とデザイン」は人間の行動や美とデザインへの接続を扱います。旧章URLは対応する3部へ解決します。

- `src/EvolutionCourse.jsx`：本文、文献、3部の表示と旧URLの解決。
- `src/EvolutionConnections.jsx`：自然選択と遺伝、DNAと分子進化、遺伝と血縁選択、群選択と血縁選択を切り替えて見る関係図。
- `src/EvolutionExplorers.jsx` と `src/evolutionMath.js`：自然選択とハミルトンのルールの操作図・計算。
- `src/evolution.css`：年表と関係図の表示。
- `scripts/check-evolution.mjs`：選択のモデルを独立した閉形式の計算と比較し、境界値とハミルトンのルールを検証。

構成、年代の扱い、図の意味、文献の位置づけは `../docs/evolution-editorial-notes.md` に記録しています。

## マネジメント入門

入口は `#/management`（01へ直接進みます）。次の3章で構成しています。

1. **マネジメントとは何か**（`#/management/foundations`）：語源と現在の意味、組織における定義、四つの機能、効率と有効性、ドラッカーとミンツバーグの視点。
2. **マネジメントの歴史と変遷**（`#/management/history`）：「仕事・知識」「人間・協働」「判断・責任」の三つの問いで、管理の捉え方の広がりを読む章。七つの年代区分の縦の年表に、18の代表文献が扱う問題（なぜ）と二つの要点を示します。各項目の直下で詳しい解説と書誌を開けます。
3. **現代的なマネジメントの考え方と主要トピック**（`#/management/contemporary`）：目的・成果、自律性、学習、関係者への責任という四つの視点から入り、戦略、意思決定、組織設計、動機づけ・人材育成、リーダーシップ・チーム、業務運営・成果評価、知識創造・組織学習、イノベーション・変革、倫理・社会的責任の九領域を見渡します。各領域に中心となる問い、必要な理由、二つの要点を示し、関連文献とその位置づけを直下のトグルで開けます。

年表の縦線は年代順を示し、色とラベルは三つの問いのうち主な論点を表します。02の末尾では、各時代に加わった視点と、標準化・統制から協働・判断・学習の条件の設計へと広がる議論の重心を概観します。02には自分の活動への応用課題を置かず、歴史の概観から03へ進みます。年代区分は教材上の整理で、理論の交代時期や直接の影響関係を示すものではありません。年は原著・原論文の刊行年（ホーソン研究のみ研究期間）。狭い画面では、年代を各区分の見出しの上へ配置します。旧7章のURLは対応する新しい章に読み替えます。

- `src/ManagementCourse.jsx`：章の表示、主要トピックと文献案内。
- `src/ManagementHistory.jsx`：縦の年表と、その場で開く文献解説。
- `src/managementIntroduction.js`：語源から始まる導入本文と概念の比較表。
- `src/managementContemporary.js`：現代的な考え方、九つの主要トピック、関連文献の位置づけ。
- `src/managementEpochs.js`：七つの年代区分、転換点、18項目の概要と論点。
- `src/managementContent.js`：章の構成、歴史を概観する本文、年表から参照する文献解説。
- `src/managementSources.js`：原著の書誌、確認先、著者紹介、読むポイント。
- `src/management.css`：マネジメント教材内の表示。

書誌の年は原著の年を基本とし、再刊版・翻訳・講演にリンクする場合は区別して明記しています。引用箇所やページ番号を追加する際は、利用する版の本文を確認してください。

## 心理学入門

`#/psychology` から01を直接開きます。次の4章です。

1. **心理学は何を調べるか**：分野、操作的定義、信頼性・妥当性。通知と読解を仮の問いとして、何を比較・記録するかを考えます。
2. **考え方と研究方法の歴史**：精神物理学、実験室、記憶、機能主義・精神分析、心理測定、行動主義、ゲシュタルト心理学、社会・発達、認知、人間性心理学、脳の指標、日常・オンライン、追試と公開まで。「問い／支えた計測・記録技術／どう調べたか／記録・比較できるもの」で読む18項目の年表です。反応時間計測器・脳波・加算平均・ERP成分の研究・視線・fMRI・fNIRS・スマートフォンなどの技術進化も重ねています。加算平均の項目には合成波形を1・8・32試行で平均する説明図があります。年代は代表的な節目で、学派の交代年ではありません。
3. **実験・調査を組み立てる**：方法の比較表、12の計測方法の仕組み・手順・記録の数値例・単位と基準・計算と読み方（心電図と光電容積脈波法は別項目、fNIRSを含む）、ERP成分を調べる場面ごとに読む説明、ストループ課題の提示例、参加者内・参加者間計画、順序・割付、研究の6手順。フィラー・妨害刺激・統制条件・盲検化・実験協力者・目的の開示を区別し、欺瞞の代替、倫理審査、同意、中止、デブリーフィングを説明します。アッシュはダミー・実験協力者を使う例として説明します。ミルグラムとスタンフォード監獄実験は独立した「実験の注意点」に置き、心理的負担・中止の自由・研究者の介入を検討します。
4. **心理効果・現象の一覧**：知覚・注意、記憶・学習、判断・意思決定、対人・集団、動機づけ・情動、発達、研究・測定の80項目を検索できます。「まず押さえる／分野ごとに押さえる／発展」の学習優先度を色と文字で示し、分野と優先度で絞り込めます。名称・現象を読み、比較方法・注意点・文献を開く形式です。重要度と研究結果の確かさは区別しています。

- `src/PsychologyCourse.jsx`：本文、ストループ課題の提示例、心理効果の一覧。
- `src/psychologyContent.js`：章・年表・心理効果・出典。
- `src/PsychologyEffects.jsx`、`src/psychologyAdditionalEffects.js`、`src/psychologyEffectSources.js`：効果一覧・学習優先度・追加項目と出典。
- `src/PsychologyMethods.jsx`・`src/psychologyMeasurements.js`：12の計測方法を個別に開閉するトグル（初期状態は全閉）。生データのファイル例→必要な処理→処理後の結果の順にまとめています。欺瞞と研究倫理、古典的実験の読み方も収録。
- `src/psychologyRawRecords.js`：12手法の生データの保存形式・列の意味・補正や集計の手順。fNIRSの光強度とERPの連続脳波・イベント時刻を、処理済みの値と区別します。
- `src/psychologyMeasurementRecords.js`・`src/PsychologyFnirsExample.jsx`：12手法の記録形式・数値の基準と、開始前を0としたfNIRSの架空の波形。CW方式の相対変化、光路長に依存する単位、条件差の読み方。
- `src/PsychologyErpComponents.jsx`：C1・P1・N1・N170・MMN・P300・N400・ERNの課題、時刻の目安、解釈の注意。
- `src/PsychologyErpExample.jsx`・`src/psychologyErp.js`：合成波形を使った加算平均の図。実測や診断ではありません。
- `src/psychologyMath.js`：参加者ごとの条件平均と平均差。
- `src/psychology.css`：教材内の年表・表・図の表示。
- `public/data/psychology/`：架空の試行CSVと、標準ライブラリだけで動くPythonコード。
- `scripts/build-psychology-example.py`：架空データを生成し、配布コードの実行出力を `src/psychologyExample.json` に保存。
- `scripts/check-psychology.mjs`：参加者単位の集計、CSV、Python出力、出典・検索の確認。`npm run check` から実行します。

数値は実測や効果の証拠ではなく、計算用の架空データです。反応時間は正答から計算し、誤答率は別に報告します。標本数を推奨したり、提示例で能力を判定したりする教材ではありません。注意点と文献、Pythonの詳細は最初は閉じています。

## 脳科学入門

`/neuroscience` から「細胞と信号」を直接開きます。続いて「回路と認知機能」「脳科学とデザイン」「いまだにわかっていないこと」の4章です。歴史ではなく現在の知見を軸とし、01では接続パターンとニューロンの種類から発火・可塑性へ進みます。「多くのニューロンは複数の入力を統合して発火する」を基本に置き、シナプスの説明に続いて空間的・時間的加重を図解します。02では部位名を直接示す脳地図と「位置・主な働き・つながり」を対応させ、ミラーニューロンによる行為の観察と実行の関係、記憶や注意などの認知機能へ進みます。04は「正直、多くのことが分かっていません。」を前提に、細胞と回路、知覚と身体、学習と記憶、行動と他者、情動と思考、意識と発達の6分野・15の問いを並べます。問いを開くと手がかりとなる知見と未解明のメカニズムを読めます。視床の詳しい回路図は04の閉じた補足に置いています。03は入力の加重という共通原理を土台に連想を考え、「アイデアをよく出そうと思ったら、情動回路を発火させないと、連想が起こらない」という著者の発言を仮説として引用します。部位・経路によるLTPなどの特性の違い、情動・記憶・皮質の接続図、海馬損傷の知見を、メカニズムを考える根拠として置きます。著者の増幅モデルでは入力パターンに応じた学習の痕跡が前提になることを明記します。パターン補完は02の記憶の補足、扁桃体刺激は03の補足に置きます。

- `src/NeuroscienceCourse.jsx`：本文・章構成・ルート
- `src/NeuroscienceUnknowns.jsx`：04の6分野・15の未解明の問い
- `src/NeuronFoundations.jsx`：01の収束・発散・N:M・再帰的な回路図、ニューロンの種類、入力の加重
- `src/NeuroscienceBrainMap.jsx`：02の外側・中央付近・側頭葉の内側の脳地図と、対応する説明
- `src/EmotionMemoryNetwork.jsx`：情動・記憶・皮質の接続図と、経路の切り替え
- `src/NeuroscienceExplorers.jsx`：視床の回路図
- `src/NeuroscienceFigures.jsx`：noteの図と拡大表示、シナプス・グリア細胞・脳の部位の図解
- `public/images/neuroscience/`：参考記事から許可を得て再利用した図（元画像のまま）
- `src/neuroscienceSources.js`：出典と参考記事
- `src/neuroscience.css`：教材の図と比較欄の表示

参考記事は蘆澤雄亮「3-04 脳の構造とデザイン」。記事内の仮説と、論文や基礎資料で確認できる知見を区別しています。動物実験には対象と課題を添え、回路図の矢印・抑制・機能間の関係を区別して記載しています。

## 経済学入門

`/economics` から01「経済学の基本」を直接開きます。02「経済学の成立と展開」、03「ミクロ経済学」、04「マクロ経済学」、05「経済学の研究アプローチ」の5章です。

歴史を序盤の柱とし、年代の横に転換点と主要概念を表示します。問いと説明方法の変化を、現代のミクロ・マクロや実証研究へつなげます。ケインズ以後は、戦後のケインズ派、マネタリズム、新しい古典派、RBC、ニューケインジアン、新しい新古典派総合、日本のリフレをめぐる議論を扱います。学派と政策の考え方を区別し、需要・期待・価格調整の関係から説明します。研究の章では、理論モデル、統計・計量、因果推論、実験・自然実験、構造モデルによる政策評価を、具体的な比較の作り方から説明します。

参考記事は蘆澤雄亮「3-07 デザインのミクロとマクロ」。標準的な入門の基礎を主とし、ゲーム理論は含めません。デザインとの接続を扱う章は設けません。対話図の数値は学習用の架空例で、前提を図の近くに記載します。補足は初期状態で閉じています。

- `src/EconomicsCourse.jsx`：基本・ミクロ・マクロと章ルート。
- `src/EconomicsHistory.jsx`、`src/economicsPostKeynes.js`：歴史、ケインズ以後の学派、ニューケインジアンの関係図、リフレと実質金利の説明。
- `src/EconomicsMethods.jsx`：研究アプローチ。
- `src/EconomicsShared.jsx`、`src/economicsSources.js`：共通表示と出典。
- `src/EconomicsExplorers.jsx`：予算、需要・供給、名目・実質GDP、倹約、差の差の対話図。
- `src/economicsMath.js`、`scripts/check-economics.mjs`：数値モデルと計算の検証。
- `src/economics.css`：図・年表・表の表示。
- `../docs/economics-editorial-notes.md`：前提と編集上の判断。

## 科学入門

`/science` から01を直接開きます。01「科学とは何か？」、02「科学はどう知識をつくるのか？」、03「科学の歴史と考え方の変遷」、04「現代の科学と主な論点」の4章です。

SEPを基本文献とし、蘆澤（2026）「『科学』と『面白い研究』の条件」の第2節を、科学史から現代の設計研究へつなぐ問題提起として扱います。02は演繹・帰納・アブダクションを一つの三角図で切り替え、A→B→CとA→Cのどのつながりを導くかを示します。アブダクションではA→BとB→Cの両方を仮説として補う例を示し、片方が既知の場合もあることを明記します。図の構造は提供資料『「新しい論文のかたち」討論会用メモ』12頁、例はPeirce（1878）を参考に再構成し、観察の量だけで帰納とアブダクションを区別しないようSEPと照合しています。03では線引き問題の前史と基準の変遷、およびPure／Appliedの変遷を、年代が上から下へ進む二つの年表で示します。検証可能性・反証可能性・クーン・ラカトシュ・ラウダン・複数基準・価値判断をめぐる新たな線引きを、背景・転換点・残る問いで説明します。専門用語は初出の本文と年表内で説明し、検証・反証・反証可能性は豆の例で区別します。パラダイム・通常科学・研究プログラム・Mode 1／2などの基本的な意味は、閉じた補足に入れません。Stokesの二軸は選択式の図で示し、理解と利用の両立を学べます。04は案内表示の架空例を使って、説明・予測・方法の提案で必要な証拠がどう変わるかを比較します。続いてResearch through Design（RtD）を、制作・試行・振り返りを通して問いを捉え直し知識を生む研究として説明します。Frayling（1993/94）、Zimmermanほか（2007）、Gaver（2012）を参照し、道を選べる案内の架空例で、試作品と研究上の知識の関係を示します。03から04のRtD節へ直接移動できます。

- `src/ScienceCourse.jsx`：本文・章構成・出典表示・ルート
- `src/scienceHistory.js`：二つの年表と文献上の留意点
- `src/ScienceFigures.jsx`：三つの推論の共通図、パスツールの象限、研究目的別の比較
- `src/scienceSources.js`：参照文献と読むポイント
- `src/science.css`：教材と図の表示

文献の補足は初期状態で閉じています。本文と図は独自の学習用整理で、SEPの翻訳転載ではありません。人物の立場、歴史研究の対象範囲、著者の問題提起を区別します。Mode 2やPure／Appliedの変化を、科学全体の一斉の置き換えとして扱いません。各章はビルド時に固有タイトル・OG情報を持つHTMLになります。

## デザイン学入門

`/design` は01「中心となる核がない学問」へ直接進みます。全6章です。02は地域で分けず、インハウスデザインの誕生も含めて歴史をたどります。03「使う人からデザインを考える」は、ニールセンのユーザビリティを前段に置き、アフォーダンスとシグニファイア、ゲシュタルト心理学の応用、利用者の多様性と参加へ進みます。10原則はユーザビリティ節内の補足です。04はUXです。05「デザイン思考と、調査・試作・評価」に、Crossのデザイン思考1／2の区別と論争、5つの活動を相互に結ぶ図、ダブルダイヤモンド、調査から試作・評価の具体例をまとめています。06は研究です。旧 `/design/dialogue` は03へ、旧 `/design/inquiry` は05（`/design/process`）へ統合し、従来のURLでも統合後の内容を表示します。

- `src/DesignStudyCourse.jsx`：本文の表示、問いで切り替える知識の図、UX・メンタルモデル・手がかり・デザイン思考の5つの活動・ダブルダイヤモンドの図。
- `src/designStudyContent.js`・`src/designStudyExperience.js`・`src/designStudyUsability.js`：章の本文。
- `src/DesignPerceptionFigures.jsx`：ゲシュタルト心理学の原理と応用例、ニールセンの10原則。
- `src/designStudyHistory.js`：年代順の出来事。
- `src/designStudySources.js`：著者記事と原論文・機関資料の参照先。

蘆澤雄亮の3本の記事を構成の参考にし、史実・用語は原資料と照合します。JSSDの3例は研究の幅を読むための選例です。論文収集作業のPDF・抽出テキスト・APIデータは取り込まず、書誌リンクと教材用の短い説明のみを掲載しています。
